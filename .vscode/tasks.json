{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "nx run build --no-tui",
            "problemMatcher": "$tsc",
            "label": "nx: compile",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "type": "shell",
            "command": "nx run watch",
            "problemMatcher": "$tsc-watch",
            "label": "nx: watch",
            "isBackground": true
        }
    ]
}
