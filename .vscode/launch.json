{
    "configurations": [
        {
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "${workspaceFolder}/examples/env-vars"
            ],
            "name": "Run extension (env-vars)",
            "outFiles": [
                "${workspaceFolder}/dist/**/*.js"
            ],
            "preLaunchTask": "nx: watch",
            "request": "launch",
            "type": "extensionHost"
        },
        {
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "${workspaceFolder}/examples/settings"
            ],
            "name": "Run extension (settings)",
            "outFiles": [
                "${workspaceFolder}/dist/**/*.js"
            ],
            "preLaunchTask": "nx: watch",
            "request": "launch",
            "type": "extensionHost"
        }
    ],
    "version": "0.2.0"
}
