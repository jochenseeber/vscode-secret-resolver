{
    "configurations": [
        {
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "${workspaceFolder}/examples"
            ],
            "name": "Run Extension",
            "outFiles": [
                "${workspaceFolder}/dist/**/*.js"
            ],
            "preLaunchTask": "nx: watch",
            "request": "launch",
            "type": "extensionHost"
        },
        {
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "${workspaceFolder}/examples"
            ],
            "name": "Run Extension",
            "outFiles": [
                "${workspaceFolder}/dist/**/*.js"
            ],
            "preLaunchTask": "nx: compile",
            "request": "launch",
            "type": "extensionHost"
        }
    ],
    "version": "0.2.0"
}
